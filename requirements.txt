# Runtime + test dependencies actually used today.
pydantic>=2.13.4,<3
pytest

# certifi: pure-Python CA bundle used only by the optional Gemini path for TLS
# verification (some Pythons ship without a usable system trust store). The code
# falls back to the system default if it is absent; no compilation.
certifi

# Added in later features when they are actually imported:
#   streamlit  -> the web UI feature (supports Python 3.10-3.14)
# The core app, full test suite, and local fallback need none of the above extras.
