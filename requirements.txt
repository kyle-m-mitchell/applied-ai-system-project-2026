# Community Cloud reads the root requirements file, so this is the reproducible
# development + deployed-UI environment.  Use requirements-core.txt for a lean
# CLI-only install.
-r requirements-core.txt

pytest>=9.1.1,<10
streamlit==1.60.0
